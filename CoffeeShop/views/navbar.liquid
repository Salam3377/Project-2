{% block content %}
  <nav class=" navbar navbar-expand-md  navbar-dark bg-dark">
    <div class="container-fluid " >

      <a href="/menu" class="navbar-brand">Menu</a>
      
      <a href="/" class="navbar-brand" id="main-page">Main Page</a>
      
      {% if loggedIn %}
        {%- comment -%} <a href="/examples/mine" class="nav-link text-light">Hello
          {{ username }}
        </a> {%- endcomment -%}
        {%- comment -%} <a href="/cart/mine" class="navbar-brand">Cart</a> {%- endcomment -%}
      {% endif %}
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <a href="/examples/mine" class="nav-link text-light">Hello
          {{ username }}
        </a>
        <a href="/cart/mine" class="navbar-brand">Cart</a>
        {% if loggedIn %}
          {%- comment -%} <a href="/examples" class="nav-link text-light">View All</a> {%- endcomment -%}
          <a href="/cart/new" class="nav-link text-light">New</a>
          {%- comment -%} <a href="/examples/mine" class="nav-link text-light">My Examples</a> {%- endcomment -%}
          <a href="/auth/logout" class="nav-link text-light">Log Out</a>
        {% else %}
          <a href="/auth/signup" class="nav-link text-light">Sign Up</a>
          <a href="/auth/login" class="nav-link text-light">Sign In</a>
        {% endif %}
      </div>
    </div>
  </nav>
{% endblock %}